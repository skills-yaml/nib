version = 1

# These entries mirror nib's bundled OpenRouter suggestions. They deliberately remain
# unapproved until a maintainer validates exact canonical membership, current tool
# support, price, and regional availability against an authenticated OpenRouter catalog
# snapshot. Paid modes fail closed while any entry remains unapproved.

[[model]]
id = "openai/gpt-5.6-sol"
approved = false
transports = ["chat_completions"]
required_scenarios = ["complete_text", "streamed_text", "single_tool_continuation"]
required_parameters = ["tools"]
rationale = "Bundled OpenAI-family coverage; pending first authenticated live review."
owner = "nib-maintainers"
reviewed_at = "2026-08-06"
expires_at = "2027-02-06"

[[model]]
id = "anthropic/claude-opus-5"
approved = false
transports = ["chat_completions"]
required_scenarios = ["complete_text", "streamed_text", "single_tool_continuation"]
required_parameters = ["tools"]
rationale = "Bundled Anthropic-family coverage; pending first authenticated live review."
owner = "nib-maintainers"
reviewed_at = "2026-08-06"
expires_at = "2027-02-06"

[[model]]
id = "google/gemini-3.6-flash"
approved = false
transports = ["chat_completions"]
required_scenarios = ["complete_text", "streamed_text", "single_tool_continuation"]
required_parameters = ["tools"]
rationale = "Bundled Gemini-family coverage; pending first authenticated live review."
owner = "nib-maintainers"
reviewed_at = "2026-08-06"
expires_at = "2027-02-06"

[[model]]
id = "x-ai/grok-4.5"
approved = false
transports = ["chat_completions"]
required_scenarios = ["complete_text", "streamed_text", "single_tool_continuation"]
required_parameters = ["tools"]
rationale = "Bundled xAI-family coverage; pending first authenticated live review."
owner = "nib-maintainers"
reviewed_at = "2026-08-06"
expires_at = "2027-02-06"
