version = 1

# Reviewed against authenticated OpenRouter catalog on 2026-09-25
# (Actions run 36116388282, revision 5b95533379bee0986848ce4456105117f2342da6).

[[model]]
id = "openai/gpt-5.6-sol"
approved = true
transports = ["chat_completions"]
required_scenarios = ["complete_text", "streamed_text", "single_tool_continuation"]
required_parameters = ["tools"]
required_input_modalities = ["text"]
required_output_modalities = ["text"]
rationale = "Exact OpenRouter id in 2026-09-25 catalog; text in/out and tools present."
owner = "nib-maintainers"
reviewed_at = "2026-09-25"
expires_at = "2027-03-25"

[[model]]
id = "anthropic/claude-opus-5"
approved = true
transports = ["chat_completions"]
required_scenarios = ["complete_text", "streamed_text", "single_tool_continuation"]
required_parameters = ["tools"]
required_input_modalities = ["text"]
required_output_modalities = ["text"]
rationale = "Exact OpenRouter id in 2026-09-25 catalog; text in/out and tools present."
owner = "nib-maintainers"
reviewed_at = "2026-09-25"
expires_at = "2027-03-25"

[[model]]
id = "google/gemini-3.6-flash"
approved = true
transports = ["chat_completions"]
required_scenarios = ["complete_text", "streamed_text", "single_tool_continuation"]
required_parameters = ["tools"]
required_input_modalities = ["text"]
required_output_modalities = ["text"]
rationale = "Exact OpenRouter id in 2026-09-25 catalog; text in/out and tools present."
owner = "nib-maintainers"
reviewed_at = "2026-09-25"
expires_at = "2027-03-25"

[[model]]
id = "x-ai/grok-4.5"
approved = true
transports = ["chat_completions"]
required_scenarios = ["complete_text", "streamed_text", "single_tool_continuation"]
required_parameters = ["tools"]
required_input_modalities = ["text"]
required_output_modalities = ["text"]
rationale = "Exact OpenRouter id in 2026-09-25 catalog; text in/out and tools present."
owner = "nib-maintainers"
reviewed_at = "2026-09-25"
expires_at = "2027-03-25"
